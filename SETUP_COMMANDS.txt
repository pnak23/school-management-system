╔══════════════════════════════════════════════════════════════════════════════╗
║  DATATABLES + JQUERY + SWEETALERT2 STACK - COMMANDS TO RUN                  ║
╚══════════════════════════════════════════════════════════════════════════════╝

📦 STEP 1: Install Backend Package (Composer)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

composer require yajra/laravel-datatables-oracle:"^11.0"


📦 STEP 2: Publish Configuration (Optional)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

php artisan vendor:publish --tag=datatables


📦 STEP 3: Install Frontend Packages (NPM)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

npm install jquery datatables.net datatables.net-dt datatables.net-responsive datatables.net-responsive-dt sweetalert2 --save


📦 STEP 4: Build Assets
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For DEVELOPMENT (with hot reload):
npm run dev

For PRODUCTION (minified, optimized):
npm run build


📦 STEP 5: Clear Caches (if needed)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear


📦 STEP 6: Test the Setup
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Open in browser:
http://127.0.0.1:8000/admin/sample-users

Expected: A table with users loaded via Ajax, with search, sort, and delete functionality.


╔══════════════════════════════════════════════════════════════════════════════╗
║  QUICK TROUBLESHOOTING                                                        ║
╚══════════════════════════════════════════════════════════════════════════════╝

If DataTable doesn't load:
1. Make sure "npm run dev" is running in a terminal
2. Hard refresh browser: Ctrl + Shift + R
3. Check browser console for errors

If jQuery is undefined:
1. npm install jquery --save
2. npm run dev
3. Refresh browser

If routes don't work:
1. php artisan route:clear
2. php artisan route:list | grep sample-users


╔══════════════════════════════════════════════════════════════════════════════╗
║  FILES MODIFIED (FOR REFERENCE)                                               ║
╚══════════════════════════════════════════════════════════════════════════════╝

✅ Created:
   - app/Http/Controllers/Admin/SampleUserTableController.php
   - resources/js/modules/datatable-helpers.js
   - resources/views/admin/sample_users/index.blade.php
   - DATATABLES_STACK_SETUP.md

✅ Modified:
   - routes/web.php (added sample routes)
   - resources/js/app.js (added jQuery, DataTables, SweetAlert2)
   - resources/views/layouts/app.blade.php (added @stack('styles'))


╔══════════════════════════════════════════════════════════════════════════════╗
║  REUSABLE PATTERN FOR NEW MODULES                                             ║
╚══════════════════════════════════════════════════════════════════════════════╝

1. Controller:
   - Add getData() method
   - Use: return DataTables::of($query)->make(true);

2. Route:
   - Route::get('module/data', [Controller::class, 'getData'])->name('module.data');

3. Blade:
   - Copy pattern from sample_users/index.blade.php
   - Change table ID, columns, and Ajax URL

4. JavaScript:
   - Use: initServerSideDataTable(selector, url, columns)
   - Use: confirmDelete(id, url, table, itemName)


╔══════════════════════════════════════════════════════════════════════════════╗
║  FULL DOCUMENTATION: DATATABLES_STACK_SETUP.md                                ║
╚══════════════════════════════════════════════════════════════════════════════╝



║  DATATABLES + JQUERY + SWEETALERT2 STACK - COMMANDS TO RUN                  ║
╚══════════════════════════════════════════════════════════════════════════════╝

📦 STEP 1: Install Backend Package (Composer)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

composer require yajra/laravel-datatables-oracle:"^11.0"


📦 STEP 2: Publish Configuration (Optional)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

php artisan vendor:publish --tag=datatables


📦 STEP 3: Install Frontend Packages (NPM)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

npm install jquery datatables.net datatables.net-dt datatables.net-responsive datatables.net-responsive-dt sweetalert2 --save


📦 STEP 4: Build Assets
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

For DEVELOPMENT (with hot reload):
npm run dev

For PRODUCTION (minified, optimized):
npm run build


📦 STEP 5: Clear Caches (if needed)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear


📦 STEP 6: Test the Setup
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Open in browser:
http://127.0.0.1:8000/admin/sample-users

Expected: A table with users loaded via Ajax, with search, sort, and delete functionality.


╔══════════════════════════════════════════════════════════════════════════════╗
║  QUICK TROUBLESHOOTING                                                        ║
╚══════════════════════════════════════════════════════════════════════════════╝

If DataTable doesn't load:
1. Make sure "npm run dev" is running in a terminal
2. Hard refresh browser: Ctrl + Shift + R
3. Check browser console for errors

If jQuery is undefined:
1. npm install jquery --save
2. npm run dev
3. Refresh browser

If routes don't work:
1. php artisan route:clear
2. php artisan route:list | grep sample-users


╔══════════════════════════════════════════════════════════════════════════════╗
║  FILES MODIFIED (FOR REFERENCE)                                               ║
╚══════════════════════════════════════════════════════════════════════════════╝

✅ Created:
   - app/Http/Controllers/Admin/SampleUserTableController.php
   - resources/js/modules/datatable-helpers.js
   - resources/views/admin/sample_users/index.blade.php
   - DATATABLES_STACK_SETUP.md

✅ Modified:
   - routes/web.php (added sample routes)
   - resources/js/app.js (added jQuery, DataTables, SweetAlert2)
   - resources/views/layouts/app.blade.php (added @stack('styles'))


╔══════════════════════════════════════════════════════════════════════════════╗
║  REUSABLE PATTERN FOR NEW MODULES                                             ║
╚══════════════════════════════════════════════════════════════════════════════╝

1. Controller:
   - Add getData() method
   - Use: return DataTables::of($query)->make(true);

2. Route:
   - Route::get('module/data', [Controller::class, 'getData'])->name('module.data');

3. Blade:
   - Copy pattern from sample_users/index.blade.php
   - Change table ID, columns, and Ajax URL

4. JavaScript:
   - Use: initServerSideDataTable(selector, url, columns)
   - Use: confirmDelete(id, url, table, itemName)


╔══════════════════════════════════════════════════════════════════════════════╗
║  FULL DOCUMENTATION: DATATABLES_STACK_SETUP.md                                ║
╚══════════════════════════════════════════════════════════════════════════════╝









